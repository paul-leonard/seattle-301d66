<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tasks</title>
</head>
<body>
  <div>
    <ol>
      <% toDoList.forEach(task => { %>
        <li>
          <h2><%= task.title %></h2>
          <p><%= task.description %></p>
          <a href="/task/<%= task.id %>">See Details</a>
        </li>
      <% }) %>
    </ol>
  </div>
</body>
</html>